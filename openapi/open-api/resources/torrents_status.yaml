get:
  summary: returns a list of possible torrent status
  tags:
    - torrent
  security:
    - bearerAuth: []
  responses:
    '200':
      description: ok
      content:
        application/json:
          schema:
            type: array
            items:
              type: string